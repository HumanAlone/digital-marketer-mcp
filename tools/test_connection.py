from mcp_instance import mcp


@mcp.tool()
def test_connection() -> str:
    """
    Проверяет работоспособность MCP сервера и доступность инструментов.

    Выполняет базовую диагностику сервера, проверяет корректность
    инициализации и доступность всех зарегистрированных инструментов.

    Returns:
        Строка с результатом проверки подключения

    Raises:
        RuntimeError: При критических ошибках инициализации

    Examples:
        >>> status = test_connection()
        >>> print(status)
        "✅ MCP сервер Яндекс.Директ работает"
    """
    return "✅ MCP сервер Яндекс.Директ работает. Доступны инструменты анализа и мониторинга."
